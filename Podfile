# Uncomment the next line to define a global platform for your project
# platform :ios, '13.0'

target 'PackPalPlus' do
  use_frameworks!

  # 👉 Firebase Core + Authentication (추가할 서비스에 따라 추가 가능)
  pod 'FirebaseCore'
  pod 'FirebaseAuth'
end



